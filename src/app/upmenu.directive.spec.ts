import { UpmenuDirective } from './upmenu.directive';

describe('UpmenuDirective', () => {
  it('should create an instance', () => {
    const directive = new UpmenuDirective();
    expect(directive).toBeTruthy();
  });
});
